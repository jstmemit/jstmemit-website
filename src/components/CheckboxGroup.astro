---
import Checkbox from "./Checkbox.astro";
const { checkboxes } = Astro.props;
---

<ul class="flex flex-wrap max-w-[80vw] md:flex-row gap-x-4 gap-y-2 justify-center md:gap-12 text-left">
    {checkboxes.map((checkbox) => (
        <Checkbox label={checkbox.label} />
    ))}
</ul>